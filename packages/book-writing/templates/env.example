# @toolforge/book-writing — Configuration
# Copy to .env and fill in your keys.

# LLM provider (required for generateOutline, writeChapter, reviewConsistency)
LLM_PROVIDER=openai
LLM_API_KEY=sk-...

# (Optional) Default model
LLM_MODEL=gpt-4o
