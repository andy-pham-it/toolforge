---
standard: c4-model
version: 1.0.0
variables:
  - systemName
  - userRole
  - externalSystems
  - containers
  - components
  - deployment
  - relationships
---

# C4 Model: {{ systemName | default("<System Name>") }}

> **C4 Model** (https://c4model.com)
> Generated by SDLC Workflows — review and adjust.

## Context (Level 1)
```mermaid
graph TD
    User[{{ userRole | default("<User Role>") }}] --> System[{{ systemName | default("<System Name>") }}]
{% for ext in externalSystems %}    System --> {{ ext.name }}[{{ ext.name | default("<External System>") }}]
{% endfor %}
```

## Containers (Level 2)
```mermaid
graph TD
    User[{{ userRole | default("<User Role>") }}] --> WebApp[{{ containers.webApp | default("<Web App>") }}]
    WebApp --> API[{{ containers.api | default("<API/Service>") }}]
    API --> DB[({{ containers.database | default("<Database>") }})]
    API --> Queue[({{ containers.queue | default("<Message Queue>") }})]
```

## Components (Level 3)
### {{ containerName | default("<Container Name>") }}
```mermaid
graph TD
{% for comp in components %}    {{ comp.name }}[{{ comp.name }}] --> {{ comp.target }}[{{ comp.target }}]
{% endfor %}
```

## Deployment (Level 4)
{% for item in deployment %}- **{{ item.container }}** runs on {{ item.infrastructure }}
{% endfor %}

## Key Relationships
| Source | Destination | Protocol | Data | Frequency |
|---|---|---|---|---|
{% for rel in relationships %}| {{ rel.source }} | {{ rel.destination }} | {{ rel.protocol | default("HTTP") }} | {{ rel.data }} | {{ rel.frequency | default("real-time") }} |
{% endfor %}

{% include "_footer" %}
