name: "Doc Health — full audit"
input:
  mockAnswers:
    - q: "Check which docs?"
      a: "all"
expectedOutput:
  format: "health report table"
  contains:
    - "PRD"
    - "✅"
    - "❌"
    - "version"
    - "Template drift"
