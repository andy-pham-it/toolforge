# @toolforge/pm-support — Configuration
# Copy to .env and fill in your keys.

# LLM provider (used by skills that generate content via LLM)
LLM_PROVIDER=openai
LLM_API_KEY=sk-...

# (Optional) Default currency for invoice calculations
DEFAULT_CURRENCY=USD
