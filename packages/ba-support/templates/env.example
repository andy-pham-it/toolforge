# @toolforge/ba-support — Configuration
# Copy to .env and fill in your keys.

# LLM provider (required for crawlCompetitor, analyzePricing, swotAnalysis, generateReport, trackTrends)
LLM_PROVIDER=openai
LLM_API_KEY=sk-...

# (Optional) Default model
LLM_MODEL=gpt-4o
